# VeloxVPN 逐项验收矩阵

日期：2026-08-03（Asia/Shanghai）

> 矩阵只记录脱敏证据，不包含服务器口令、管理/订阅令牌、节点密码、UUID、私钥或实际订阅地址。`等待` 项在取得直接证据前不得判为通过。

## 构建与隔离部署

| 原计划要求 | 状态 | 直接证据 |
| --- | --- | --- |
| 独立目录 `/opt/veloxvpn-test`、独立服务 `veloxvpn-test` | 通过 | systemd 单元、工作目录和运行文件均隔离；服务保持 disabled |
| 固定 Rust 版本的官方容器构建并保存日志 | 通过 | `rust:1.88-bookworm` release 构建成功；构建日志保存在测试目录报告区 |
| `cloudflared` 仅随测试目录提供 | 通过 | 独立二进制位于测试目录；未替换系统文件 |
| Web UI 仅监听 `127.0.0.1:18080` | 通过 | 公网机套接字检查只见回环监听；内置健康检查实际访问成功 |
| 三个随机固定端口冲突检查、真实绑定重试及重启持久化 | 通过 | TCP/UDP 套接字实测、重启前后端口一致；真实抢占端口回归验证重新分配并持久化 |
| 不修改既有 sing-box/1Panel/Python 服务及其端口 | 通过 | 部署和故障测试均只操作 `veloxvpn-*` 测试单元与测试目录；既有服务未停止或改配 |
| Windows 发起真实公网客户端测试 | 通过 | 固定 sing-box 客户端经三个独立 SOCKS 入站完成 VLESS/AnyTLS/TUIC 公网转发 |
| 验收后停止、禁用并释放测试端口 | 等待 | 只能在 24 小时窗口和最终复核通过后执行 |

## 自动化与配置

| 原计划要求 | 状态 | 直接证据 |
| --- | --- | --- |
| 构建、全部测试、格式检查、严格 Clippy | 通过 | release 构建通过；11 个库测试、3 个 CLI 测试、19 个端到端测试通过；fmt 与 `-D warnings` 通过 |
| 首次生成与二次加载保持端口/令牌不变 | 通过 | 配置单元测试直接比较两次加载结果 |
| 损坏 JSON、端口冲突 | 通过 | 配置单元测试分别验证拒绝损坏 JSON 和已占用端口 |
| 非法 UUID、缺失密码、重复节点名、非法 SNI/ALPN | 通过 | `invalid_inbounds_are_rejected` 覆盖全部场景 |
| 错误 UUID/密码/WS 路径/订阅令牌、未授权管理请求 | 通过 | 协议与 Web 负向端到端测试均验证拒绝 |
| Web 测试拆分 | 通过 | 登录/状态、账户、订阅、节点生命周期、持久化及失败回滚为独立用例 |
| 网络测试超时、监听任务等待退出、临时目录清理 | 通过 | 网络拨号/往返有硬超时；监听任务 abort 后 await；用例末清理独立临时目录 |

## 公网协议

| 原计划要求 | 状态 | 直接证据 |
| --- | --- | --- |
| VLESS WebSocket、Quick Tunnel、订阅导入、TCP 转发、隧道重建 | 通过 | Windows 外部客户端实测；服务重启及 Cloudflare 故障恢复后重新建立隧道 |
| AnyTLS TCP、TLS/ALPN、正确/错误密码、域名/IPv4、大文件 | 通过 | 公网协议用例和 16 MiB 传输实测；错误密码被拒绝 |
| TUIC QUIC/UDP、TCP、UDP DNS/回显、错误认证、重连 | 通过 | 公网 TCP/UDP 测试、错误认证及服务重启重连均通过 |
| 固定版本 sing-box 与 Clash 客户端兼容 | 通过 | sing-box 1.12.19/1.13.15 实测；Mihomo 1.19.29 实际加载完整 Clash 配置成功 |
| 重启后端口、凭据、订阅持久化；旧订阅失效 | 通过 | 配置前后直接比较；订阅重生成后旧路径/令牌返回失败 |

## 稳定性与性能

| 原计划要求 | 状态 | 直接证据 |
| --- | --- | --- |
| 直连基线及三协议连接/吞吐/CPU/RSS/失败率 | 通过 | 16 MiB 交错测量、systemd 资源记录和连接统计已写入脱敏报告 |
| 20 条持续并发、50 条短时峰值 | 通过 | 三协议在两个并发档均完成 100/100 公网请求 |
| 30 分钟压力测试 | 通过 | VLESS 560/560、AnyTLS 597/600、TUIC 560/560；无崩溃或重启 |
| 24 小时低负载稳定性 | 等待 | 冻结候选服务器窗口约 08:05、Windows 窗口 08:05:37 开始；第 10 分钟三协议均 11/11、服务器稳态 RSS 基线 9,220 KiB、重启 0 |
| TUIC 1%/3% 丢包 | 通过 | 隔离网络命名空间测试；TCP 均 100/100，UDP 最终均 100/100 |
| 成功率 ≥99.5%、吞吐 ≥直连 70%、零崩溃、RSS 增长 ≤10% | 部分通过 | 吞吐、压力成功率与零崩溃已通过；最终成功率及 RSS 增长等待 24 小时结果 |
| 端口、DNS、Cloudflare、不可写配置、证书、退出信号故障 | 通过 | 六类故障均做实际注入；Cloudflare 缺失时 health=1，恢复后 health=0 |

## 优化验收

| 优先级 | 原计划要求 | 状态 | 直接证据 |
| --- | --- | --- | --- |
| P0 | 公开状态脱敏 | 通过 | 未认证状态不含订阅路径或令牌；隧道未就绪不会误报健康 |
| P0 | 无默认口令、Argon2id、过期会话、日志脱敏 | 通过 | 随机首次凭据、旧默认口令轮换、8 小时会话及日志/事件回归 |
| P0 | 配置/证书/私钥权限与原子保存 | 通过 | 公网权限为服务用户 `0600`；写盘失败保持内存与磁盘旧值 |
| P0 | 节点候选先验证、失败保留旧节点 | 通过 | 冲突时旧监听任务 ID、配置和监听数量均保持不变 |
| P0 | 随机端口实际绑定检查与重试 | 通过 | 真实抢占竞态回归通过 |
| P0 | 限速、大小限制、常量时间比较 | 通过 | 登录/订阅限速、64 KiB 请求上限及令牌比较实现与负向测试 |
| P1 | fmt 与 16 项 Clippy 清零 | 通过 | 当前 fmt、严格 Clippy 通过 |
| P1 | TUIC 具名状态、有界队列、背压 | 通过 | 有界通道、片段/会话上限及 128 流回归 |
| P1 | 节点运行状态分层 | 通过 | 管理状态区分 configured/starting/listening/failed 与 tunnel 状态 |
| P1 | 入站/cloudflared 统一取消、等待、超时 | 通过 | 5 秒停止超时、kill-on-drop、30 秒隧道就绪超时与事件记录 |
| P1 | `--config`/位置参数一致、README UTF-8 | 通过 | CLI 两种形式均实现；Windows/Linux 中文显示检查通过 |
| P2 | 缓冲区、队列和并发限制调优 | 通过 | VLESS/TUIC/AnyTLS 限制与窗口按公网并发结果调整，并有 128 并发回归 |
| P2 | UI 失败提示、实际状态、日志过滤、敏感遮罩 | 通过 | 公网 UI/API 实测；事件缓冲有界且不含凭据 |
| P2 | 健康检查、systemd、备份恢复、升级说明 | 通过 | 内置 `health` 命令与 `docs/OPERATIONS.md` |

## 报告与收尾

| 原计划要求 | 状态 | 直接证据 |
| --- | --- | --- |
| 生成不含敏感信息的环境、版本、用例、指标、失败和结论报告 | 部分通过 | `docs/ACCEPTANCE_REPORT.md` 已持续记录；最终 24 小时指标和结论待写入 |
| 停止并禁用服务、释放端口、保留复测制品 | 等待 | 待最终窗口和质量门通过后执行 |
| 更换已暴露 root 密码，优先 SSH 密钥并关闭 root 密码登录 | 等待所有者输入 | 禁止在未验证新密钥/新口令可登录前自行锁断服务器访问 |
